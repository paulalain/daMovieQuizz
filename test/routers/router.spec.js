/*global beforeEach, describe, it, assert, expect  */
'use strict';

describe('Router Router', function () {

    beforeEach(function () {
        this.RouterRouter = new DaMovieQuizz.Routers.Router();
    });

    it('index route', function(){
    	assert(true);
    });

});
